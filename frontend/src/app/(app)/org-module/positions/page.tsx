import { StubPage } from '@/components/org-module/ui';

export default function PositionsPage() {
  return <StubPage title={"Positions"} description={"Every seat in the org — a position exists even when vacant (stub)."} bullets={["Position ID, title, department, level, reports-to, status, incumbent","Filters by status and department"]} />;
}
