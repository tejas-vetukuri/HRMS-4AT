import { StubPage } from '@/components/org-module/ui';

export default function LocationTransfersPage() {
  return <StubPage title={"Location Transfers"} description={"Moves between locations with effective dates (stub)."} />;
}
