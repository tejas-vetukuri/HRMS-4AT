import { StubPage } from '@/components/org-module/ui';

export default function PreboardingPage() {
  return <StubPage title={"Preboarding"} description={"Candidates between offer and day one, by stage (stub)."} bullets={["Stage tabs","DOJ, position and org assignment","Document / BGV readiness"]} />;
}
