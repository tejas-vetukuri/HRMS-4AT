import { StubPage } from '@/components/org-module/ui';

export default function DeptTransfersPage() {
  return <StubPage title={"Department Transfers"} description={"Moves between departments with effective dates (stub)."} />;
}
