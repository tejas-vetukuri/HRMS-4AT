import { StubPage } from '@/components/org-module/ui';

export default function OnboardingPage() {
  return <StubPage title={"Onboarding"} description={"New-joiner progress against templates and checklists (stub)."} bullets={["Progress per joiner","Owners, tasks and status"]} />;
}
