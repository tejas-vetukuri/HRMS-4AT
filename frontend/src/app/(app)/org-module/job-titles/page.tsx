import { StubPage } from '@/components/org-module/ui';

export default function JobTitlesPage() {
  return <StubPage title={"Job Titles / Designations"} description={"Master list of designations used across positions (stub)."} bullets={["Title, code and level mapping","Active / inactive flag"]} />;
}
