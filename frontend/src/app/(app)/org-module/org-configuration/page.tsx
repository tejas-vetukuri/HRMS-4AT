import { StubPage } from '@/components/org-module/ui';

export default function OrgConfigurationPage() {
  return <StubPage title={"Org Configuration"} description={"Top-level hierarchy configuration (stub)."} bullets={["Legal entity → BU → location / department → team → position → employee"]} />;
}
