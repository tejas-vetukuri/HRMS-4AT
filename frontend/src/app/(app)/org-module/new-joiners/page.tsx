import { StubPage } from '@/components/org-module/ui';

export default function NewJoinersPage() {
  return <StubPage title={"New Joiners"} description={"Day-one joiners moving into onboarding (stub)."} bullets={["Joiner list with start dates","Buddy and manager assignment"]} />;
}
