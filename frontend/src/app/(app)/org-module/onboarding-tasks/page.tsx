import { StubPage } from '@/components/org-module/ui';

export default function OnboardingTasksPage() {
  return <StubPage title={"Tasks / Templates"} description={"Reusable onboarding checklists and task templates (stub)."} />;
}
