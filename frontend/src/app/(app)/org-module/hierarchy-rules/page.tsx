import { StubPage } from '@/components/org-module/ui';

export default function HierarchyRulesPage() {
  return <StubPage title={"Hierarchy Rules"} description={"Relationship and span rules for the hierarchy (stub)."} />;
}
