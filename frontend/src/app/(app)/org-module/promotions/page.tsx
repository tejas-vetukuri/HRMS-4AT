import { StubPage } from '@/components/org-module/ui';

export default function PromotionsPage() {
  return <StubPage title={"Promotions"} description={"Effective-dated promotion history with audit trail (stub)."} bullets={["From / to, effective date, status, changed by","Mock rows live on the Overview screen"]} />;
}
