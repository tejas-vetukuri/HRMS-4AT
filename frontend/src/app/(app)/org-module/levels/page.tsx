import { StubPage } from '@/components/org-module/ui';

export default function LevelsPage() {
  return <StubPage title={"Levels"} description={"Career levels shared across families (stub)."} bullets={["Level ladder (e.g. L1–L7)","Experience range per level"]} />;
}
