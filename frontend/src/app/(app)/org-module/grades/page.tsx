import { StubPage } from '@/components/org-module/ui';

export default function GradesPage() {
  return <StubPage title={"Grades / Bands"} description={"Compensation bands attached to levels (stub)."} bullets={["Band ranges per level","Currency and effective dates"]} />;
}
