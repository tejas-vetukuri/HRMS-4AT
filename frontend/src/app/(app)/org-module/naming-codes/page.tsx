import { StubPage } from '@/components/org-module/ui';

export default function NamingCodesPage() {
  return <StubPage title={"Naming / Codes"} description={"Code conventions for every master (stub)."} bullets={["Prefix and sequence per master","Examples: IN-LE-01, BU-PRD, DEPT-ENG"]} />;
}
