import { StubPage } from '@/components/org-module/ui';

export default function JobFamiliesPage() {
  return <StubPage title={"Job Families"} description={"Browse job families; positions roll up to a family (stub)."} bullets={["Family list with codes","Titles grouped under each family"]} />;
}
