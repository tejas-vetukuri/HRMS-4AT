import { StubPage } from '@/components/org-module/ui';

export default function BgvPage() {
  return <StubPage title={"Background Verification"} description={"Verification cases and document readiness (stub)."} bullets={["Case status per candidate","Vendor handoff state"]} />;
}
