import { StubPage } from '@/components/org-module/ui';

export default function ManagerChangesPage() {
  return <StubPage title={"Manager Changes"} description={"Reporting-line changes with effective dates (stub)."} />;
}
