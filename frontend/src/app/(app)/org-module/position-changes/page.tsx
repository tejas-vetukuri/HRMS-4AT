import { StubPage } from '@/components/org-module/ui';

export default function PositionChangesPage() {
  return <StubPage title={"Position Changes"} description={"Title / level / reports-to changes with effective dates (stub)."} />;
}
